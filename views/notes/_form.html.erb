<%= form_for([@problem, @problem.notes.build]) do |f| %>

  <p>
    <%= f.hidden_field :responder, value: @current_user %>
  </p>
  <p>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
